เพิ่มใน ESP32_WaterCheck_v1.ino:

#include "SystemMonitor.h"

ใน setup() หลัง storageLoad():
systemMonitorBegin();
systemMonitorRestartBoot();

ใน loop() บรรทัดแรก:
systemMonitorLoopStart();

ใน loop() หลัง ArduinoOTA.handle():
systemMonitorWifiConnected(WiFi.isConnected());

ก่อน delay(2):
systemMonitorLoopEnd();

หมายเหตุ: Sprint 1.7 เพิ่ม SystemMonitor เป็นโมดูลแยก เพื่อไม่ทับไฟล์ .ino ที่รวมจาก Sprint 1.6.1
